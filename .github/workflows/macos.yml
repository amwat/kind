name: Go
on: [push]
jobs:

  build:
    name: Build
    runs-on: [windows-latest]
    steps:

      - name: Run docker
        run: |
          choco install docker-desktop
          choco install kind
          kind create cluster

